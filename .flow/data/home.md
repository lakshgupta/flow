---
id: home
type: home
title: Home
---

## Backlog

- Improvements
    
- Fix
    
    - if I add the mermaid diagram at the last line then I am unable to move the cursor to the right side of the mermaid section and then on pressing enter my cursor should be on the next line. similarly, if I add the mermaid diagram at the first line of the page then I am unable to move the cursor to the left side of the mermaid section and then on pressing enter the whole mermaid section should be moved on the next line
        
    - each graph canvas should maintain the coordinates of the nodes individually. otherwise what is happening is that updating a position of a node in the sub graph also affects the position of the same node in the parent graph.
        
- Tagging
    
    - ability to add a tag using `#` trigger. typing `#` and typing without any space should show user options to select an already used tags or type in a new one fully. the tag could be added anywhere in the page.
        
    - deleting a tag should delete it from the memory of the app in case it was the last of it's kind. for example, if there is no longer a tag name 'test' in any of the page then the `#` trigger should not show `test` as an option to select.
        
- Search
    
    - simplify the global search option. I would still like to be able to search with either or and condition with the title, tags, description or content. Is there a way to use a single search bar instead of multiple as used currently?//
        
    - Is there a way to combine the graph local title search with the global search? currently there are 2 search bars visible when graph view is open which seems unintuitive.
        
- Skills
    
    - fix [skills.md](http://skills.md) file generated by flow. Install [skills.md](http://skills.md) file automatically in the global space - ~/.agents/skills/ — delivered (20260805-001-FEAT-skills-packaging-init, commit 6a6f882): `flow skill init` writes all embedded skills to `~/.agents/skills/` (or `.agents/skills/` with `--project`)
        
    - refine skills- summarize, link with related work
        

## Thinking

- Command node
    
    - finish implementing command type of nodes
        
- Task node
    
    - enable status tracking on the tasks
        
- Draw on the canvas directly
    
    <p><br></p>
