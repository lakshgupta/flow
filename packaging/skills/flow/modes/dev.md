# Mode: Development

The `dev` mode is the canonical skill itself: composition returns the full SKILL.md unchanged, including the complete stage-routing table and all stage workflows (design, plan, implement, fix, refactor, test, review, commit) plus graph engineering.

This file documents that contract for `flow skill list` and future maintainers; it is not spliced into composed output.
